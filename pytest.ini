[pytest]
# Anchors pytest's rootdir here. Without this, pytest walks up to Group/pyproject.toml,
# which belongs to an unrelated project and carries a UTF-8 BOM that fails TOML parsing.
testpaths = tests
pythonpath = .
